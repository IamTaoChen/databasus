
# docker/agent Directory Guide

This directory contains Docker build and runtime configuration files for the Databasus Agent.

## File Structure

- `compose.yml`: Example Docker Compose configuration for the Agent service.
- `Dockerfile`: Dockerfile for building the Agent image.
